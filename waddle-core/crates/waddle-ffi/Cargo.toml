[package]
name = "waddle-ffi"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "libwaddle: the C ABI over waddle-runtime (cdylib). UNSTABLE until consumed by two real frontends (N5)"

[lib]
name = "waddle"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
waddle-runtime = { workspace = true }
waddle-types = { workspace = true }
waddle-gate = { workspace = true }
prost = { workspace = true }

[lints]
workspace = true

[dev-dependencies]
tempfile = { workspace = true }
waddle-gate = { workspace = true }
